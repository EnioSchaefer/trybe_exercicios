const queenMoves = 'forward, backward, diagonals, sideways.';
let chessPiece = 'queen';

if (chessPiece === 'queen') {
    console.log(queenMoves);
} else {
    console.log('invalid piece')
}

// nao fiz mais pecas por falta de tempo ;)